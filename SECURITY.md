# 安全策略

## 支持的版本

| 版本 | 支持 |
|---|---|
| 最新发布版 | ✅ |
| 其他版本 | ❌ |

请先升级到最新发布版，再确认问题是否仍然存在。

## 私密报告漏洞

不要把可利用的漏洞、令牌、原始日志或详细复现步骤发布到公开 Issue。

优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密报告
入口。如果仓库页面没有该入口，请先通过 GitHub 私下联系维护者，说明希望进行安全
协调，不要在公开讨论中粘贴敏感细节。本项目不在文档中公布未经确认的邮箱地址。

报告中可以先提供：

- 受影响的发布版本和 Windows 版本；
- 简短影响描述；
- 可安全共享的最小复现步骤；
- 是否需要本地文件、恶意 WonderfulDb 数据、局域网访问或特定安装状态。

请删除真实账户 openid、完整本地路径、快传 URL/令牌、访问令牌和未脱敏日志后再
发送附件。

## 安全边界

- ACLOS `WonderfulDb`、`snapshot<openid>` 和 ACLOS 的身份缓存是输入数据；应用以只读
  方式读取，不能修改 ACLOS、游戏、Riot、WeGame 或 Vanguard 文件。
- 本地视频、日志和截图路径会在后端按固定边界校验。路径来自已登记的本地资料库时，
  才能被播放、定位、截图或快传；渲染器不能把这些命令变成任意文件访问。
- 应用当前没有遥测、账号同步或后台上传。资料库索引、账户显示偏好和诊断日志保留在
  本机应用目录。
- 正式版本启动后的更新检查只访问项目 GitHub Releases 的 HTTPS `latest.json`。更新
  安装器使用签名校验；私钥不在仓库中。
- 快传是用户主动开启的临时局域网 HTTP 服务，固定使用 `22357/TCP`、随机令牌和
  Host 校验。Windows 防火墙规则只允许已安装程序从本地子网访问；企业策略、第三方
  防火墙和路由器隔离仍可能拒绝连接。
- 常规浏览使用本地索引和随包资源，但项目不能被描述成“完全不联网”。

## 不要提交的内容

请勿提交：

- 任何 API key、OAuth/JWT、密码、签名私钥或 `.secret_key`；
- 真实 WonderfulDb、snapshot、ACLOS Local Storage、日志或视频文件；
- 真实 openid、账户昵称与标签的固定映射；
- 包含用户目录、NAS 位置、快传令牌或网络地址的截图和测试夹具。

安全边界的实现细节见 [架构说明](docs/ARCHITECTURE.md)、[ACLOS 数据格式](docs/ACLOS_FORMAT.md)
和 [自更新系统](docs/UPDATER.md)。
