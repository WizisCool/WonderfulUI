# WonderfulUI 产品定义

## 一句话

WonderfulUI 是 Windows 上的无畏契约个人高光资料库：它把 ACLOS 已经生成在本机的
高光、对局和事件整理起来，让玩家不用启动游戏或录制客户端也能找到并回看自己的时刻。

## 用户

目标用户是使用腾讯 ACLOS/“无畏时刻”生成高光的无畏契约玩家。他们的电脑里已经有
多个账户、数十或数百个本地视频，但不想为了找一段高光重新登录客户端或启动游戏。

## 产品承诺

用户打开应用后，应能快速回答三个问题：

1. 这段高光属于哪个账户、哪场对局、什么时间？
2. 我能否按表现、结果、英雄、地图或日期找到它？
3. 我能否在本机直接回看、截图，或主动发给同一局域网的另一台设备？

## 当前能力

- 账户列表、账户顺序和 WonderfulUI 内部账户显示名；
- 对局列表、搜索、地图/英雄/模式/胜负/视频类型/MVP-SVP/日期筛选；
- 击杀、KDA 和视频数量等表现范围筛选；
- 对局详情、视频分组、内置播放器和事件时间轴；
- 播放器截图、逐帧查看、在资源管理器中定位视频；
- 资料库概览、增量扫描和全量扫描；
- 用户主动发起的局域网二维码快传；
- GitHub Releases 应用内更新提示。

## 数据边界

ACLOS `WonderfulDb` 和可选的 `snapshot<openid>` 是数据来源，不是产品身份。它们
决定了 WonderfulUI 能显示哪些对局、昵称和部分 MVP/SVP 信息。应用对这些文件保持
只读；解析失败或数据缺失时应给出可恢复的提示，而不是伪造内容。

WonderfulUI 的 SQLite 索引、账户显示偏好和诊断日志属于应用自己的本地数据。当前
没有遥测、账号同步、后台上传或云资料库。

“本地优先”描述的是资料库和核心浏览体验，不是网络承诺。正式版本会检查 GitHub
Releases 更新；用户开启快传时会在局域网启动临时 HTTP 服务。产品文案必须把这两种
行为说清楚，不能写“完全不联网”或“纯离线”。

## 明确不做的事

当前版本不是：

- 游戏录制器或直播工具；
- 云相册、同步服务或远程资料库；
- ACLOS/WonderfulDb 编辑器或通用解析框架；
- 支持删除、编辑高光、导出、备份、恢复或手动切换数据目录的文件管理器；
- MSI 安装包或便携解压版。

这些边界不是待办承诺。若未来增加能力，应先更新产品定义、用户文档和安全边界，
再修改实现。

## 体验原则

### 先看自己的内容

列表、封面、账户和统计优先呈现真实数据。不要用大段框架介绍、虚构指标或营销词
占据用户第一次看到的界面。

### 三步找到并回看

账户/筛选 → 对局详情 → 播放器是主要路径。加载、扫描和缺失文件要提供短而具体的
状态，不把内部错误堆栈推给普通玩家。

### 只读但不含糊

UI 可以告诉用户正在读取 ACLOS、索引本地视频或写入 WonderfulUI 自己的 SQLite，
但不能暗示会修改游戏文件。缺失数据时，宁可说明来源或路径状态，也不要猜测。

### 分享由用户发起

快传只服务于用户当前选择的视频。它使用带令牌的临时局域网地址，关闭播放器后应
结束服务；产品叙事不应把它描述成云端上传。

## 文案基调

中文、直接、克制，面向普通玩家。优先使用“整理资料库”“找到对局”“回看高光”“本地
读取”等具体动词，避免“革命性”“极致”“强大”、框架说明书口吻和过多 emoji。
