---
name: WonderfulUI
description: Local-first personal Valorant highlight library for Windows.
colors:
  signal-red: oklch(0.62 0.21 25)
  signal-red-bright: oklch(0.68 0.22 25)
  ember: oklch(0.30 0.06 25)
  ash-white: oklch(0.98 0.01 25)
  bunker: oklch(0.16 0.012 30)
  panel: oklch(0.19 0.012 30)
  panel-raised: oklch(0.22 0.014 30)
  panel-top: oklch(0.25 0.014 30)
  line: oklch(0.30 0.014 30)
  line-faint: oklch(0.26 0.012 30)
  field-white: oklch(0.94 0.01 30)
  dispatch-gray: oklch(0.78 0.012 30)
  intel-gray: oklch(0.58 0.014 30)
  shadow-gray: oklch(0.40 0.012 30)
  victory: oklch(0.72 0.16 145)
  victory-dim: oklch(0.28 0.06 145)
  defeat: oklch(0.66 0.18 25)
  defeat-dim: oklch(0.28 0.08 25)
  alert: oklch(0.78 0.14 75)
typography:
  body:
    fontFamily: "MiSans, MiSansLatin, Inter, system-ui, sans-serif"
    fontSize: 14px
    fontWeight: 330
    lineHeight: 1.5
  label:
    fontFamily: "MiSans, MiSansLatin, Inter, system-ui, sans-serif"
    fontSize: 13px
    fontWeight: 380
  mono:
    fontFamily: "JetBrains Mono, Cascadia Code, Consolas, Menlo, monospace"
    fontSize: 12px
    fontWeight: 450
rounded:
  sm: "3px"
  md: "6px"
  lg: "8px"
  xl: "10px"
  pill: "999px"
  circle: "50%"
spacing:
  gap: "4px"
  pad: "16px"
---

# WonderfulUI 设计系统

## 设计目标

WonderfulUI 的设计服务于一个很具体的动作：打开自己的高光资料库，找到某场对局，
然后回看其中的片段。它应像一个安静、可靠的个人档案柜，而不是营销首页、游戏启动
器或数据分析 SaaS。

产品身份是“个人高光资料库”。ACLOS/WonderfulDb 是只读数据来源和兼容条件，不能
成为界面里的主标题、品牌标语或功能价值描述。

## 视觉方向

- **深色暖灰底色**：长时间浏览和观看时保持稳定，避免纯黑的空洞感。
- **单一红色锚点**：红色只标识当前选择、主要操作、焦点或高光状态，不作为装饰背景。
- **平面层级**：用四级表面明度区分账户、列表、详情和浮层，少用阴影和玻璃拟态。
- **真实内容优先**：封面、地图、英雄、时间和统计来自资料库；空状态不使用虚构的
  赛博插画或指标。
- **桌面优先**：清晰焦点环、稳定的键盘操作、可预期的 Esc/右键/双击行为比炫目的
  页面转场更重要。

## 色彩

CSS 变量位于 `packages/gui/src/assets/style.css`，本文件只记录语义：

| 语义 | 当前变量 | 用途 |
|---|---|---|
| 页面底色 | `--bg` | 应用最底层背景 |
| 普通表面 | `--surface` | 面板、顶部栏、列表 |
| 抬升表面 | `--surface-2` / `--surface-3` | 选中项、统计块、浮层 |
| 主文字 | `--ink` | 标题、当前值、正文 |
| 次级文字 | `--ink-2` / `--ink-3` | 描述、时间、辅助信息 |
| 主强调色 | `--accent` | 主要按钮、选择、焦点、事件标记 |
| 胜负语义 | `--win` / `--loss` | 必须同时配合文字或图标 |
| 警告 | `--warn` | 缺失数据和错误状态 |

规则：

1. 一屏只保留一个主要红色焦点；不要让所有可点击元素都变红。
2. 胜负不能只用颜色表达，必须有“胜/败”文字或其它明确形状。
3. 纯灰色会破坏暖色底，新增中性色应沿用现有暖灰色相。
4. 颜色 token 优先于组件内的临时 hex/rgba；截图和 canvas 例外需要写清原因。

## 字体和信息密度

- 中文和界面文字使用 MiSans/MiSansLatin，按 `--font-sans` 回退。
- KDA、时间戳、文件大小和路径使用 `--font-mono`，中文标签不要使用等宽字体。
- 基础字号是 14px；`--step-*` 是固定的界面层级，不应暗示当前存在用户字号设置。
- 标题、按钮、标签和数据的重量使用 `--w-medium`、`--w-semibold`、`--w-bold`。
- 信息密度由真实字段和间距控制，不通过缩小到难以阅读的字体来塞内容。

## 页面结构

主界面从左到右是：

1. **账户栏**：全部账户、账户顺序、账户显示名和数量；
2. **筛选栏**：搜索、分类筛选、日期和表现范围；
3. **对局列表**：按时间倒序的真实对局卡片；
4. **详情栏**：对局统计、视频类型、事件和播放入口。

设置、更新、事件列表、播放器和快传是按需出现的模态或浮层。播放器优先提供应用
内回看；“在系统播放器中打开”是明确的降级入口，不应隐藏在不透明的自动行为中。

## 组件约定

### 列表和卡片

- 封面是真实地图/英雄/视频资源；资源缺失时显示文字或简单 glyph，不制造假缩略图。
- 账户、对局、视频和事件的标题要能脱离颜色单独理解。
- 选中、悬停、键盘焦点和错误状态要有不同的视觉反馈。
- 列表的虚拟滚动、稳定 DOM 和 `ROW_HEIGHT` 约束见
  [前端约定](docs/FRONTEND_CONVENTIONS.md)，不要在设计调整时另造一套规则。

### 播放器

- 视频区域是视觉主角，控制条只在需要时出现。
- 事件标记使用清楚的击杀/死亡语义和可访问标签；不要用密集装饰覆盖视频。
- 截图只捕获当前视频画面；截图保存由系统 Save As 对话框承载。
- 快传明确显示“同 Wi-Fi/局域网”和当前视频状态，不使用“上传到云端”等词。

### 设置、更新和日志

- 设置中的资料库概览、扫描、日志和关于页是维护入口，不是产品首页。
- 更新弹窗说明正在检查、下载还是安装；未知下载体积必须保持不确定进度状态。
- 日志面向排障，展示脱敏后的有限内容，不把内部路径和错误堆栈当作用户文案。

## 动效和交互

- 动效只表达出现、选择、加载和状态变化；常规过渡保持短促，避免页面演示感。
- 应用按自身规则控制动效，并通过 Tauri 窗口参数保持 WebView 渲染稳定；不要在各组件
  零散引入互相冲突的 `prefers-reduced-motion` 分支。
- 模态层级和 Escape 归属以 `utils/modal-layer.ts` 为准：快传、更新、设置、播放器、
  事件列表由上到下拥有关闭权。
- 右键菜单、复制链接、截图和快传等动作应有明确的成功/失败反馈。

## 无障碍

- 所有按钮和图标按钮都有可理解的 `aria-label` 或可见文字。
- 键盘焦点使用 2px 可见焦点环；不能只靠 hover 或颜色表示焦点。
- 对话框使用 `role="dialog"`、`aria-modal` 和焦点边界；关闭时恢复触发元素。
- 进度条区分已知百分比和 indeterminate 状态；未知总量时不伪造 `0%`。
- 日期、胜负、MVP/SVP、击杀/死亡等信息同时提供文字和语义，不只使用颜色或图标。

## 文案规则

优先使用“资料库”“高光”“对局”“回看”“读取”“刷新”“快传”。ACLOS/WonderfulDb
在系统要求、首次使用、故障排查和技术文档中出现，在用户 Hero 中只作为必要前置。

避免：

- “革命性”“极致”“强大”等无法验证的形容词；
- “完全不联网”“纯离线”“不连接任何远程服务器”；
- 删除、编辑、导出、备份、手动目录、云同步等未实现能力；
- 把浏览器 mock、静态检查或单元测试写成 Windows 真机验收。

## 取舍

设计系统不规定每个页面都必须有插画、截图或动画。没有真实、无敏感数据的视觉材料
时，宁可使用清晰的文字和真实界面，也不要加入占位截图或样板营销区块。
